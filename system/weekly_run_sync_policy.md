# Weekly Run Sync Policy

- weekly_run は `preview -> backup -> adopt` で同期する。
- デフォルトは preview のみ。`ADOPT_WEEKLY_RUN=1` の時だけ更新する。
- 採用時は `backups/weekly_run/` にバックアップを保存する。
- basic手順は残し、「今週の運用方針」「推奨コマンド例」セクションだけ更新する。
