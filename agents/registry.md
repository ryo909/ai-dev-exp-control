# Agents Registry（エージェント台帳）
新規追加は「既存で代替不能」かつ「週1で再利用される」場合のみ。
追加したら必ずここに登録し、使われないものは deprecated にする。

- name:
  purpose:
  inputs:
  outputs:
  forbidden:
  when_to_use:
  status: active|deprecated
  notes:
