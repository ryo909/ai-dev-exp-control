interface:
  display_name: "Evidence Collector"
  short_description: "visual evidence report generation"
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "playwright"
      description: "Optional for visual capture"
