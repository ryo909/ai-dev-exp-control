interface:
  display_name: "Competitor Scan"
  short_description: "shortlist上位URLの構成抽出→twist強化"
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "playwright"
      description: "Browser automation for competitor scan"
    - type: "mcp"
      value: "openaiDeveloperDocs"
      description: "Primary source checks when needed"
